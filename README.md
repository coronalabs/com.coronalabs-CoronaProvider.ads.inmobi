Sources for the plugin `CoronaProvider.ads.inmobi`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.ads.inmobi" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
